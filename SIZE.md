# Size & line-count report

> **Auto-generated by `pnpm run measure`.** Every number here is reproducible —
> the commands are at the bottom. The headline metric is **gzipped bytes**; line
> counts are a readability proxy, not a benchmark.

## Headline

| metric | value |
| --- | --- |
| **Gzipped (what ships over the wire)** | **2269 bytes (~2.2 kB)** |
| Minified | 4882 bytes (~4.8 kB) |
| Compiled JavaScript (formatted) | 274 lines |
| Runtime TypeScript source | 328 lines |
| Runtime **dependencies bundled** | **0** |

A whole schema-driven forms engine — `field` / `group` / `list` / `wrap` /
`hidden` / `ZodForm` / `useZodForm`, the `resolve` introspection toolkit, and
the URL-state codecs — in **2269 gzipped bytes** with **zero** runtime
dependencies. Every `import` in the bundle is an externalized peer
(`react`, `react-hook-form`, `zod`, `@hookform/resolvers`) that the host app
already ships; nothing third-party is inlined.

## Source breakdown (cloc, comments & blanks excluded)

| file | role | code lines |
| --- | --- | --- |
| [`src/insane.tsx`](src/insane.tsx) | the runtime engine — all logic | 328 |
| [`src/types.ts`](src/types.ts) | type surface — **compile-time only, emits nothing** | 114 |
| [`src/index.ts`](src/index.ts) | public barrel | 18 |
| **total** | | **460** |

The type module costs **0 runtime bytes**: it is `import type` throughout, so it
disappears at compile time. That is why the compiled JavaScript (274 lines)
is *smaller* than the runtime TypeScript (328 lines) — type
annotations and JSX collapse away.

## Why 2269 gzipped bytes, not fewer

There is no filler to remove. 4882 minified bytes ÷ 274 lines ≈
17 bytes of irreducible tokens per line — already 1-char
identifiers and stripped whitespace. gzip then roughly halves that (the standard
~40–50% ratio for already-minified JS, which has little redundancy left to
exploit). Both numbers are exactly what real, dense logic produces.

## What actually ships

The complete, formatted, browser-targeted production bundle (peers externalized):

```js
'use client'

// src/insane.tsx
import { standardSchemaResolver } from '@hookform/resolvers/standard-schema'
import { Fragment, isValidElement, useEffect } from 'react'
import { FormProvider, useController, useFieldArray, useForm } from 'react-hook-form'
import * as z from 'zod'
import { jsx, jsxs } from 'react/jsx-runtime'
var def = (s) => s._zod.def
var resolve = (visit) =>
  function go(schema) {
    const d = def(schema)
    if (d.getter) return go(d.getter())
    const hit = visit(schema, d)
    if (hit !== void 0) return hit
    return d.innerType ? go(d.innerType) : void 0
  }
var resolveMeta = (key) => resolve((s) => s.meta()?.[key])
var resolveComponent = (s) => resolveMeta('component')(s)
var resolveTitle = (s) => resolveMeta('title')(s)
var resolveDescription = (s) => resolveMeta('description')(s)
var resolveInner = (s) => resolve((n, d) => (d.innerType ? void 0 : n))(s) ?? s
var resolveDefault = (s) =>
  resolve((_n, d) =>
    d.type === 'default' || d.type === 'prefault'
      ? typeof d.defaultValue === 'function'
        ? d.defaultValue()
        : d.defaultValue
      : void 0,
  )(s)
var containsWrapper =
  (...kinds) =>
  (s) =>
    resolve((_n, d) => (kinds.includes(d.type) ? true : void 0))(s) ?? false
var isOptional = (s) => containsWrapper('optional', 'nullable')(s)
var isReadonly = (s) => containsWrapper('readonly')(s)
var isRequired = (s) => !isOptional(s)
function queryParam(field2) {
  const fallback = resolveDefault(field2)
  if (fallback === void 0)
    throw new Error(
      'queryParam: the field needs a .default(...) \u2014 absence from the URL must map to a value.',
    )
  const kind = def(resolveInner(field2)).type
  const coerce = (raw) =>
    kind === 'boolean' ? raw === 'true' : kind === 'number' ? Number(raw) : raw
  return {
    parse: (raw) => {
      const result = field2.safeParse(coerce(raw))
      return result.success ? result.data : null
    },
    serialize: (value) => String(value),
    eq: (a, b) => a === b,
    defaultValue: fallback,
  }
}
function queryParams(schema) {
  return Object.fromEntries(
    Object.entries(schema.shape).map(([key, field2]) => [key, queryParam(field2)]),
  )
}
function useQueryParamsSync(form, push) {
  useEffect(() => {
    const subscription = form.watch((values) => void push(values))
    return () => subscription.unsubscribe()
  }, [form, push])
}
var dev = () => globalThis.process?.env?.NODE_ENV !== 'production'
var rendersOrDeferred = (s) => {
  const d = def(s)
  if (d.getter) return true
  if (s.meta()?.component) return true
  return d.innerType ? rendersOrDeferred(d.innerType) : false
}
function Render({ schema, name }) {
  const ResolvedComponent = resolveComponent(schema)
  if (!ResolvedComponent) {
    if (typeof console !== 'undefined')
      console.warn(
        `[insane] no renderer for "${name || '(root)'}" \u2014 attach one via field(), group(), list(), or hidden().`,
      )
    return null
  }
  return /* @__PURE__ */ jsx(ResolvedComponent, {
    schema,
    inner: resolveInner(schema),
    name,
    required: isRequired(schema),
    readonly: isReadonly(schema),
  })
}
var BareShell = ({ children }) => /* @__PURE__ */ jsx(Fragment, { children })
function field(spec) {
  if (spec.schema) return annotateLeaf(spec.schema, spec)
  return (schema) => annotateLeaf(schema, spec)
}
function annotateLeaf(schema, spec) {
  const ShellC = spec.shell ?? BareShell
  const widget = spec.widget
  const Leaf = (p) => {
    const { field: f, fieldState } = useController({
      name: p.name,
      // per-field draft seeding: declared .default() wins, else the explicit initial
      defaultValue: resolveDefault(p.schema) ?? spec.initial,
    })
    const props = {
      name: p.name,
      value: f.value,
      onChange: f.onChange,
      onBlur: f.onBlur,
      label: resolveTitle(p.schema),
      description: resolveDescription(p.schema),
      error: fieldState.error?.message,
      required: p.required,
      readonly: p.readonly,
    }
    const extra = spec.props?.(p.schema) ?? {}
    return /* @__PURE__ */ jsx(ShellC, { ...props, children: widget({ ...props, ...extra }) })
  }
  return schema.meta({ component: Leaf })
}
var HiddenRenderer = () => null
var hidden = (s) => s.meta({ component: HiddenRenderer })
function group(...parts) {
  const shape = {}
  const sections = []
  const spliceKeys = (record) => {
    const keys = Object.keys(record)
    for (const k of keys) {
      if (dev() && /^\d+$/.test(k))
        console.warn(
          `[insane] numeric field name "${k}" \u2014 JS enumerates integer-like keys first, breaking authored order.`,
        )
      if (dev() && !rendersOrDeferred(record[k]))
        console.warn(
          `[insane] group: "${k}" has no renderer \u2014 annotate it via field()/group()/list()/hidden().`,
        )
      shape[k] = record[k]
    }
    return keys
  }
  for (const part of parts) {
    if (isValidElement(part)) {
      sections.push(part)
      continue
    }
    if (part instanceof z.ZodType) {
      const sub = def(part).shape ?? {}
      if (resolveComponent(part)) {
        for (const k of Object.keys(sub)) shape[k] = sub[k]
        sections.push(part)
      } else {
        sections.push(spliceKeys(sub))
      }
      continue
    }
    sections.push(spliceKeys(part))
  }
  const GroupRenderer = (p) =>
    /* @__PURE__ */ jsx(Fragment, {
      children: sections.map((sec, i) =>
        isValidElement(sec)
          ? // biome-ignore lint/suspicious/noArrayIndexKey: sections is an authored static sequence — positions never reorder at runtime
            /* @__PURE__ */ jsx(Fragment, { children: sec }, `deco:${i}`)
          : sec instanceof z.ZodType
            ? // biome-ignore lint/suspicious/noArrayIndexKey: same authored static sequence
              /* @__PURE__ */ jsx(Render, { schema: sec, name: p.name }, `frag:${i}`)
            : sec.map((k) =>
                /* @__PURE__ */ jsx(
                  Render,
                  { schema: shape[k], name: p.name ? `${p.name}.${k}` : k },
                  k,
                ),
              ),
      ),
    })
  return z.object(shape).meta({ component: GroupRenderer })
}
function wrap(Wrapper, ...parts) {
  const g = group(...parts)
  const Inner = resolveComponent(g) ?? (() => null)
  return g.meta({
    component: (p) =>
      /* @__PURE__ */ jsx(Wrapper, { children: /* @__PURE__ */ jsx(Inner, { ...p }) }),
  })
}
var BareItems = ({ items, header, footer }) =>
  /* @__PURE__ */ jsxs(Fragment, {
    children: [
      header,
      items.map((it) => /* @__PURE__ */ jsx(Fragment, { children: it.node }, it.key)),
      footer,
    ],
  })
var boundsOf = (arr) => {
  let min = 0
  let max = Infinity
  for (const c of def(arr).checks ?? []) {
    const cd = c?._zod?.def
    if (cd?.check === 'min_length' && typeof cd.minimum === 'number') min = cd.minimum
    if (cd?.check === 'max_length' && typeof cd.maximum === 'number') max = cd.maximum
    if (cd?.check === 'length_equals' && typeof cd.minimum === 'number') min = max = cd.minimum
  }
  return { min, max }
}
var seedFor = (element) =>
  resolveDefault(element) ?? (def(resolveInner(element)).type === 'object' ? {} : void 0)
function list(element, opts = {}) {
  if (dev() && !rendersOrDeferred(element))
    console.warn(
      `[insane] list: element has no renderer \u2014 annotate it via field()/group()/list()/hidden().`,
    )
  const Wrap = opts.wrapper ?? BareItems
  const ListRenderer = (p) => {
    const fa = useFieldArray({ name: p.name })
    const { min, max } = boundsOf(p.inner)
    const canAdd = fa.fields.length < max
    const canRemove = fa.fields.length > min
    const items = fa.fields.map((f, i) => ({
      key: f.id,
      // stable identity — never the index
      node: /* @__PURE__ */ jsx(Render, { schema: element, name: `${p.name}.${i}` }),
      remove: canRemove ? () => fa.remove(i) : void 0,
    }))
    return /* @__PURE__ */ jsx(Wrap, {
      label: resolveTitle(p.schema),
      items,
      add: canAdd ? () => fa.append(opts.seed ? opts.seed() : seedFor(element)) : void 0,
      header: opts.header,
      footer: opts.footer,
    })
  }
  return z.array(element).meta({ component: ListRenderer })
}
function useZodForm(schema, opts = {}) {
  const { defaults, ...rest } = opts
  return useForm({
    resolver: standardSchemaResolver(schema),
    defaultValues: defaults,
    shouldUnregister: false,
    // pinned: hidden/passthrough values must survive
    ...rest,
  })
}
function ZodForm({ schema, defaults, onSubmit, children, className }) {
  const methods = useZodForm(schema, { defaults })
  return /* @__PURE__ */ jsx(FormProvider, {
    ...methods,
    children: /* @__PURE__ */ jsxs('form', {
      className,
      onSubmit: methods.handleSubmit((d) => onSubmit(d)),
      children: [/* @__PURE__ */ jsx(Render, { schema, name: '' }), children],
    }),
  })
}
export {
  Render,
  ZodForm,
  boundsOf,
  field,
  group,
  hidden,
  isOptional,
  isReadonly,
  isRequired,
  list,
  queryParam,
  queryParams,
  resolve,
  resolveComponent,
  resolveDefault,
  resolveDescription,
  resolveInner,
  resolveMeta,
  resolveTitle,
  useQueryParamsSync,
  useZodForm,
  wrap,
}
```

## Reproduce

```sh
pnpm run measure          # regenerates this file
cloc src/insane.tsx       # runtime line count
cloc src/types.ts         # type-only line count
```
