The landing headline. The stagger is the point: left line, right line, accent only on the verb.

```jsx
<Hero lineOne="The schema" verb="is" lineTwo="the form."
      lead="…" install={<InstallCommand />} meta="zod 4 · react 19 · mit" />
```
