Use this instead of a feature grid whenever the claim is "the schema is the form" — it proves it on the page.

```jsx
<SchemaDiptych code={<CodeBlock>…</CodeBlock>} rendered={<><Field label="Name" /><CheckField label="Primary" /></>} />
```

The right column always sits on `--muted` so the two halves read as different materials.
