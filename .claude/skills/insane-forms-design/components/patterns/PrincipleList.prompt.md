Grid for wide columns, numbered for narrow ones.

```jsx
<PrincipleList layout="numbered" items={[{title:"Matchless rendering", body:"…"}]} />
```

Four items is the house length; the numbers are mono and accent.
