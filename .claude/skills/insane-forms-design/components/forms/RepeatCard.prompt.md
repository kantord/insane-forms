Wraps each item rendered by an array node, so the repetition is visible as structure.

```jsx
<RepeatCard index={1} onRemove={remove}>
  <Field label="Email" value={v} onChange={set} />
</RepeatCard>
```
