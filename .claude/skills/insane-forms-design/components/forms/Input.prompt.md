The text control a schema node's renderer returns.

```jsx
<Input value={value} onChange={(e) => onChange(e.target.value)} />
```

Fixed 42px height and a full outline on all four sides — never an underline-only variant.
