The atom of syntax highlighting. Accent steps are reserved for schema constructors and constraints.

```jsx
<CodeLine indent={2}>
  <Token step="n1">Contact</Token><Token step="a1">.array</Token><Token step="n3">()</Token>
</CodeLine>
```

Never use a1/a2 for keywords or punctuation — the accent means "this becomes UI".
