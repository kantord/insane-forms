Wrap every page in this — landing included. Pages supply only the middle column.

```jsx
<DocsShell nav={<SidebarNav>…</SidebarNav>} aside={<AsideRail>…</AsideRail>}>
  <TopBar links={links} /> <Hero … /> <FooterBar … />
</DocsShell>
```
