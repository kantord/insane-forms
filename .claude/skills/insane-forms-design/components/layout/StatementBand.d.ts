/** Inverted full-width band for the one claim a page ends on. */
export interface StatementBandProps {
  children?: React.ReactNode
  style?: React.CSSProperties
}
export function StatementBand(props: StatementBandProps): JSX.Element
