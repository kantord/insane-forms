A note that belongs beside the text, not inside it.

```jsx
<AsideNote label="Note" offset={520}>The bounds come from the schema's own .min(1).max(3).</AsideNote>
```

Never give it a tinted callout box — the rail is the container.
