Labels a section, a breadcrumb or a panel. Always mono — an eyebrow set in Archivo breaks the system's one type rule.

```jsx
<Eyebrow tone="accent">Schema</Eyebrow>
```

`accent` for the label above a panel or code block; `muted` for breadcrumbs, counters and footer meta.
