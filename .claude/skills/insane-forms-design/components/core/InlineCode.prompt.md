For an API name inside a sentence.

```jsx
<p>The form edits the <InlineCode>z.input</InlineCode> draft.</p>
```

Never give it a tinted background — in this system code is marked by family, not by a chip.
