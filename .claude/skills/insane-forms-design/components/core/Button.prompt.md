One filled control per view — use `primary` for the single real action, `outline` for a secondary path, `ghost` for inline links that need a hit area.

```jsx
<Button variant="primary" type="submit">Save</Button>
```

No radius and no shadow: the button is a rectangle of accent. Never place two primary buttons in one view — the active nav item already spends one accent fill.
